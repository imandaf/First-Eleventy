---
title: Projects
description: A showcase of my projects.
layout: layouts/base.njk
---

<h1 class="text-2xl font-bold mb-6">Projects</h1>
<div class="grid gap-6 md:grid-cols-2">
  {% for project in collections.projects %}
    <article class="border rounded-lg p-4 hover:shadow-lg transition">
      <h3 class="text-xl font-semibold mb-2">
        <a href="{{ project.url }}" class="hover:underline">{{ project.data.title }}</a>
      </h3>
      <p class="mb-2">{{ project.data.description }}</p>
      <a href="{{ project.data.repo }}" class="text-sm text-blue-600 hover:underline">View Repository</a>
    </article>
  {% endfor %}
</div>
