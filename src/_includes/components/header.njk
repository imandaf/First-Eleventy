<header class="py-6">
  <nav class="flex justify-between items-center">
    <a href="/" class="text-xl font-semibold">{{ site.title }}</a>
    <button @click="dark = !dark" class="px-3 py-1 border rounded">
      <span x-text="dark ? 'Light Mode' : 'Dark Mode'"></span>
    </button>
  </nav>
</header>